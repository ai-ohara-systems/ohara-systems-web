---
import SpaceAstronaut from './SpaceAstronaut.astro';
import SpaceMoon from './SpaceMoon.astro';
import FeatureIcon from './FeatureIcon.astro';

interface Props {
  data: any;
  lang: 'de' | 'en';
}

const { data, lang } = Astro.props;
const color = data.color;

const i18n = {
  de: {
    conceptTitle: 'Das Konzept',
    inDev: 'In Entwicklung',
    githubFollow: 'GitHub folgen',
    backLabel: 'Zurück zur Übersicht',
  },
  en: {
    conceptTitle: 'The Concept',
    inDev: 'In development',
    githubFollow: 'Follow on GitHub',
    backLabel: 'Back to overview',
  },
};

const t = i18n[lang];
---

<style>
  @keyframes heroFloat {
    0%, 100% { transform: translateY(0px) rotate(-3deg); }
    50%       { transform: translateY(-18px) rotate(3deg); }
  }
  @keyframes heroPulse {
    0%, 100% { opacity: 0.05; }
    50%       { opacity: 0.12; }
  }
  .hero-astronaut-float { animation: heroFloat 7s ease-in-out infinite; transform-origin: center; }
  .hero-glow-pulse { animation: heroPulse 4s ease-in-out infinite; }
</style>

<!-- ===== HERO ===== -->
<section class="relative overflow-hidden px-6 py-28 md:py-36">
  <!-- Pulsing background glow in brand color -->
  <div class="pointer-events-none absolute inset-0">
    <div class="hero-glow-pulse absolute top-0 left-1/2 -translate-x-1/2 h-[500px] w-[700px] rounded-full blur-3xl" style={`background: ${color}14;`}></div>
  </div>

  <!-- Floating astronaut + orbiting moon -->
  <div class="hero-astronaut-float pointer-events-none absolute top-1/2 right-[5%] -translate-y-1/2 opacity-25 hidden lg:block" aria-hidden="true">
    <div style="position:absolute;top:40px;left:80px;">
      <SpaceMoon size={60} orbitRadius={120} orbitDuration={35} />
    </div>
    <SpaceAstronaut width={180} height={198} />
  </div>

  <div class="relative mx-auto max-w-5xl">
    <div class="flex flex-col items-center text-center gap-8">
      <!-- Logo -->
      <div class="h-24 w-24 rounded-3xl flex items-center justify-center" style={`background: linear-gradient(to bottom right, ${color}33, ${color}26, ${color}1a); border: 1px solid ${color}40;`}>
        <img src={data.logo} alt={`${data.name} Logo`} width="56" height="56" class="h-14 w-14"/>
      </div>

      <!-- Badge row -->
      <div class="flex flex-wrap items-center justify-center gap-2">
        {data.badges.map((badge: any) => (
          badge.type === 'warning'
            ? <span class="rounded-full bg-amber-500/10 border border-amber-500/20 px-3 py-1 text-xs font-medium text-amber-400">{badge.text}</span>
            : <span class="rounded-full bg-white/5 border border-white/10 px-3 py-1 text-xs text-white/50">{badge.text}</span>
        ))}
      </div>

      <div>
        <h1 class="text-5xl font-bold tracking-tight text-white mb-4 md:text-6xl">
          {data.name}
        </h1>
        <p class="text-xl text-white/60 max-w-2xl leading-relaxed">
          &bdquo;{data.tagline}&ldquo;
        </p>
        <p class="mt-4 text-white/45 max-w-2xl leading-relaxed">
          {data.hero.description}
        </p>
      </div>

      <div class="flex flex-col sm:flex-row items-center gap-4">
        <a
          href={data.cta.githubUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-2 rounded-xl bg-white/8 border border-white/15 px-6 py-3 text-sm font-medium text-white hover:bg-white/12 transition-all duration-200"
        >
          <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0 0 24 12c0-6.63-5.37-12-12-12z"/></svg>
          {data.cta.githubLabel}
        </a>
      </div>
    </div>
  </div>
</section>

<!-- ===== CONCEPT ===== -->
<section class="px-6 py-20 border-t border-white/5">
  <div class="mx-auto max-w-4xl">
    <div class="text-center mb-16">
      <h2 class="text-2xl font-bold text-white mb-4">{t.conceptTitle}</h2>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {data.concepts.map((concept: any) => (
        <div class="rounded-2xl bg-[#111118] p-6" style={`border: 1px solid ${color}26;`}>
          <div class="mb-4">
            <FeatureIcon icon={concept.icon} color={concept.color} />
          </div>
          <h3 class="font-semibold text-white mb-2">{concept.title}</h3>
          <p class="text-sm text-white/50 leading-relaxed">
            {concept.description}
          </p>
        </div>
      ))}
    </div>
  </div>
</section>

<!-- ===== CTA ===== -->
<section class="px-6 py-20 border-t border-white/5">
  <div class="mx-auto max-w-2xl text-center">
    <div class="inline-flex items-center gap-2 rounded-full bg-amber-500/8 border border-amber-500/15 px-4 py-1.5 text-xs text-amber-400 mb-6">
      <span class="h-1.5 w-1.5 rounded-full bg-amber-400 animate-pulse"></span>
      {t.inDev}
    </div>
    <h2 class="text-2xl font-bold text-white mb-4">{data.cta.title}</h2>
    <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
      <a
        href={data.cta.githubUrl}
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center gap-2 rounded-xl bg-white/8 border border-white/15 px-6 py-3 text-sm font-medium text-white hover:bg-white/12 transition-all duration-200"
      >
        <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0 0 24 12c0-6.63-5.37-12-12-12z"/></svg>
        {data.cta.githubLabel}
      </a>
      <a href={data.cta.backUrl} class="text-sm text-white/40 hover:text-white/70 transition-colors">
        {data.cta.backLabel}
      </a>
    </div>
  </div>
</section>
